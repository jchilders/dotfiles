#!/usr/bin/env zsh

db_name=$1
db_dir=$HOME/work/carerev/data/dbdumps
zip_name="${db_name}_$(date +%F).gz"

echo "Dumping $db_name to $zip_name"
pg_dump $db_name | gzip -c > $zip_name
mv $zip_name $db_dir
ls -al $db_dir/$zip_name
